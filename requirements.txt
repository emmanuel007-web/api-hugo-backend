# =============================================================================
# Dependencias del Backend - NYTimes Books API Proxy (FastAPI)
# =============================================================================
# Instalar con: pip install -r requirements.txt

# Framework web asíncrono
fastapi

# Servidor ASGI de alta performance
uvicorn[standard]

# Cliente HTTP asíncrono para llamar al NYT
httpx

# Carga de variables de entorno desde .env
python-dotenv

# Validación de datos y serialización
pydantic

# Extensiones de tipos para Pydantic (requerido por FastAPI)
pydantic-settings

# Para manejo de formularios en FastAPI (opcional pero recomendado)
python-multipart
